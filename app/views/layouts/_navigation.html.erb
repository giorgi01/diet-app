<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">DietApp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if current_user %>
      <li class="nav-item active">
        <a class="nav-link" href="/meals">Meals listing<span class="sr-only">(current)</span></a>
      </li>
      <% end %>
    </ul>
  </div>
    <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
      <li class="nav-item" id="log-out">
        <p><%= link_to 'Log out', destroy_user_session_path, method: :delete %></p>
      </li>
      <% else %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li class="nav-item" id="sign-in">
        <p><%= link_to 'Sign in', user_session_path%></p>
      </li>
    </ul>
      <% end %>
</nav>